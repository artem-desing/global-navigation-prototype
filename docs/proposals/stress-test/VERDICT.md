# Stress test verdict

**Ship v0. Kill v3. Fold v2 into v0. Park v4 — but harvest its expanded mode.**

## What

One product, three states:
1. **v0** as default (rail 96 → 80 px, hover-preview 0 → 250 ms)
2. **v2's state machine** harvested into v0 as an opt-in compact-rail mode
3. **v4-expanded** preserved as a ⌘B-toggleable merged-sidebar layout

v3 dies. v2 and v4 retire as variant slugs.

## Why

- 5/7 lanes rank v0 first
- 6/7 lanes rank v3 last
- 6/7 lanes call v4-expanded the only fresh idea but v4-collapsed "v3 wearing different clothes"
- Adversarial 4-way: **v0=16, v4=12, v3=8, v2=5**
- Competitive lane: **v2 isn't a separate variant — it's v0's collapsed mode.** Every comparable vendor ships them as one compound, not two.

## What changes about the framing

The "v0 vs v2 is the main contest" frame was a category error. They aren't competitors. Treating them as one compound resolves the contradiction between the visual lane (v0 weakest at rest) and the IA/PM/competitive/adversarial lanes (v0 strongest overall).

## Next

- **Build:** stacked-swap-secondary (Sentry / Cloudflare 2025-26 pattern — uncaptured by v0–v4)
- **Then:** scope-first variant per recorded IA dissent
- **Fix regardless of variant:** AI panel focus management, group-expand persistence on product switch, tenant scope noun choice

Full reasoning: `00-synthesis.md`. Lane reports: `01`–`07`.
